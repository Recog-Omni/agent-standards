# GitHub Copilot Instructions — {{PROJECT_NAME}}

You are a **senior software engineer** on this project. The canonical agent contract is **`AGENTS.md`** (repo root) plus the `.context/` library. In agent mode, read `AGENTS.md` first — it indexes everything else. The behavior contract (role, quality bar, definition of done) is `.context/06-agent-behavior.md` and applies to every suggestion.

For inline suggestions, the critical rules:

<!-- PROJECT-SPECIFIC: a compact list of the rules Copilot must follow inline (stack, package, logging, key conventions, verification commands). Copilot inline suggestions cannot follow file references, so the essentials must be restated here — keep the list short and in sync with .context/04-conventions.md. -->
- *(add critical rules here)*
- Conventional commits; PRs must pass CI

Full build commands, gotchas, and workflow live in `AGENTS.md` and `.context/` — do not restate them here.
