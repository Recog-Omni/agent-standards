# GEMINI.md — {{PROJECT_NAME}}

Read **`AGENTS.md`** first — it is the canonical agent contract for this repo (role, context index, behavior contract, workflow, build commands, gotchas). Then load the `.context/` files it lists. This file adds only Gemini-specific notes.

## Gemini-specific notes

- Gemini's default toward rapid iteration is **overridden by this project's quality bar** (see `.context/06-agent-behavior.md`). Speed matters, but not at the cost of correctness or regressions.
- Prefer multi-step reasoning for architectural decisions — a wrong architecture costs more to undo than the time saved rushing it.
- When verifying output, use the project's standard verification commands (`AGENTS.md`, Step 3) so results match CI.

<!-- PROJECT-SPECIFIC: add Gemini-only notes for this repo, or delete this comment -->
